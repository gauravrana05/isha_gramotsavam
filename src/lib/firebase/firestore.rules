rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules - allow all reads and writes
    // WARNING: Use only during development phase
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

