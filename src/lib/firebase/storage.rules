rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow any authenticated user to read and write anything 
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
